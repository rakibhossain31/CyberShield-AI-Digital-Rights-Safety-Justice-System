# Notebooks

This folder is intentionally left with a placeholder. For a portfolio repository, keep notebooks clean and optional. The core project should run through scripts, API, tests, and dashboard.
